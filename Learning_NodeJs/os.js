// -------------------------------------
// 1. os module -- provides information about the operating system.

const os = require("os");

console.log(os.platform());
console.log(os.arch());
console.log(os.hostname());